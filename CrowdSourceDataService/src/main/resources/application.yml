spring:
  application:
    name: crowd-data-service
  data:
    mongodb:
      uri: mongodb+srv://nadeemshaik0513_db_user:qEevah6NuwFhKUzi@java.xc442yz.mongodb.net/?appName=Java
      database: Java
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/WaterQualityMonitoring
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: 8086

keycloak:
  server-url: http://localhost:8080
  realm: WaterQualityMonitoring
  client-id: main-service
  client-secret: 47uEs7i2kgUtVH4ZbMYBoR13jWW4HVJ6

feign:
  httpclient:
    enabled: true
  client:
    config:
      default:
        connectTimeout: 3000
        readTimeout: 5000
        loggerLevel: FULL
logging:
  level:
    com.waterqualitymonitoring.crowdsourcedataservice.feignclient: DEBUG
    feign: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true